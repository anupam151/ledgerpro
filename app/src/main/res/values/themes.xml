<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.LedgerPro" parent="Theme.Material3.DayNight.NoActionBar">

        <item name="colorPrimary">@color/app_theme_primary</item>
        <item name="colorPrimaryVariant">@color/app_theme_primary</item>
        <item name="android:statusBarColor">@color/app_theme_primary</item>

        <!-- Use the new padded wrapper to bypass the circular clipping -->
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="s">@drawable/splash</item>
        <item name="android:windowSplashScreenBackground" tools:targetApi="s">#192033</item>

    </style>
</resources>